# Environment Configuration
NODE_ENV=development
PORT=8080

# Database Configuration
MONGO_URI=mongodb://localhost:27017/wildspire

# Session Security (CRITICAL: Change this in production!)
SESSION_SECRET=your-secret-key-here-min-32-chars

# Cloudinary Configuration (for image uploads)
# Sign up at https://cloudinary.com/ to get these credentials
CLOUD_NAME=your-cloudinary-name
CLOUD_API_KEY=your-cloudinary-api-key
CLOUD_API_SECRET=your-cloudinary-api-secret

# Mapbox Configuration (for maps and geocoding)
# Sign up at https://www.mapbox.com/ to get this token
MAP_TOKEN=your-mapbox-public-token

# Optional: Production MongoDB (Atlas)
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/wildspire?retryWrites=true&w=majority
